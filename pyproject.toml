[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "alienapi"
version = "0.1.0"
description = "AlienAPI library"
readme = "README.md"
requires-python = ">=3.8"
dependencies = []
